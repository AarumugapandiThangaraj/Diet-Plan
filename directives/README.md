# Directives

This directory contains standard operating procedures (SOPs) written in Markdown, configuring Layer 1 of the 3-Layer Architecture.
These act as the instruction set for the orchestration layer, defining goals, inputs, tools/scripts to use, outputs, and edge cases.
